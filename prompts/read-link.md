Condense this page into one paragraph, up to {{maxChars}} characters.

What kind of page it is (article, product listing, video page, forum thread, repository, documentation, etc.), then the gist in the order the page presents it: key facts, numbers, names, dates. Quote a phrase only when it carries the point. Skip navigation, boilerplate and repeated content.

Use the page's own language if it is clear; otherwise English.

If the text is a cookie wall, a consent screen, a login gate, or has no real content, say so in at most three words.

No opinions. No markdown. Do not start with "this page".
