# Конфигурация

Все ключи `config.json` со значениями по умолчанию, по секциям.

## `features`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `dryRun` | `false` | Весь пайплайн работает, но ничего не отправляется (см. [Сухой прогон](README.md#сухой-прогон)) |
| `mentions` | `true` | Реагировать на @упоминания |
| `replies` | `true` | Реагировать на ответы |
| `nameTriggers` | `true` | Реагировать на упоминания имени в сообщениях |
| `spontaneous` | `true` | Спонтанные сообщения по случайному таймеру |
| `eavesdrop` | `true` | Случайный шанс вклиниться после любого сообщения |
| `memory` | `true` | Строить профили, отслеживать серверные паттерны, записывать факты о себе |
| `relationships` | `true` | Баллы отношения к каждому участнику (-100..100) |
| `episodes` | `true` | Долгосрочные воспоминания по каждому человеку (моменты, цитаты, обиды) |
| `lore` | `true` | Серверный лорбук |
| `reactions` | `true` | Эмодзи-реакции |
| `multiMessage` | `true` | Разрешить 2–3 сообщения подряд |
| `vision` | `true` | Обрабатывать прикреплённые картинки |
| `mediaDescriptions` | `true` | Однострочные описания для картинок, GIF, кадров видео и превью ссылок |
| `followUp` | `true` | После ответа персонажа классифицировать сообщения без обращения: продолжается ли разговор |
| `typingSimulation` | `true` | Имитировать скорость набора |
| `adminCommands` | `true` | Слэш-команды владельца; `false` отменяет их регистрацию |

## `bot`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `timezone` | `"UTC"` | Часовой пояс для меток времени, которые видит модель |
| `owners` | `[]` | ID пользователей для команд владельца |
| `commandName` | `"nep"` | Имя слэш-команды (строчные `a-z 0-9 _ -`, до 32 символов; перерегистрируется при изменении) |
| `nameTriggers` | `[]` | Дополнительные строки-триггеры помимо @упоминания |
| `guildId` | `""` | Сервер для привязки; определяется автоматически, если бот ровно на одном |
| `dryRunChannelId` | `""` | Канал для зеркала сухого прогона (см. [Сухой прогон](README.md#сухой-прогон)) |
| `channels.allow` | `[]` | Разрешённые каналы (пусто = все видимые) |
| `channels.deny` | `[]` | Игнорируемые каналы |
| `access` | `{}` | Кто кроме владельцев может запускать какие команды (управляется через `/nep access`) |

## `llm`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `baseUrl` | `"https://openrouter.ai/api/v1"` | Эндпоинт chat completions |
| `model` | `"anthropic/claude-opus-4.6"` | ID модели |
| `temperature` | `1` | Температура сэмплирования |
| `maxOutputTokens` | `700` | Макс. токенов на выходе |
| `maxRequestTokens` | `50000` | Жёсткий лимит токенов на запрос |
| `safetyMargin` | `0.9` | Доля `maxRequestTokens`, доступная бюджету |
| `timeoutMs` | `300000` | Таймаут запроса (мс) |
| `pingTimeoutMs` | `30000` | Таймаут для запросов `/nep ping` (мс) |
| `retries` | `2` | Повторы при временных ошибках |
| `maxRequestsPerDay` | `300` | Дневной лимит запросов |
| `provider` | `null` | Объект маршрутизации OpenRouter `provider`, передаётся как есть; `null` = поле не отправляется |

`llm.provider` задаёт поле маршрутизации провайдера OpenRouter в каждом запросе, например `{ "ignore": ["some-provider"] }` или `{ "order": ["anthropic"], "allow_fallbacks": true }`. Если аккаунт OpenRouter ограничивает допустимых провайдеров, исключение единственного оставшегося даст ошибку "No endpoints found" на каждом запросе. После изменения настроек провайдера выполните `/nep ping`, чтобы проверить доступность каждой роли модели.

## `context`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `channelMessages` | `100` | Сообщений из текущего канала |
| `neighborMessages` | `5` | Сообщения на каждый соседний канал |
| `neighborMaxAgeMinutes` | `60` | Макс. возраст сообщений соседних каналов (мин) |
| `neighborMaxChannels` | `8` | Макс. соседних каналов |
| `maxMessageChars` | `800` | Обрезать сообщения длиннее этого (символов) |
| `gapMarkerMinutes` | `20` | Порог маркера временного разрыва (мин) |
| `otherProfiles` | `6` | Макс. отображаемых профилей других участников |
| `askedAboutProfiles` | `3` | Сколько участников, упомянутых в недавних сообщениях, показывать полностью перед остальными |
| `tempo.liveMessages10min` | `4` | Сообщений за 10 мин = «активно» |
| `tempo.deadSilenceMinutes` | `45` | Минут тишины = «мертво» |
| `caps.interlocutor` | `6000` | Лимит токенов: профиль вызвавшего с эпизодами |
| `caps.aboutChat` | `2500` | Лимит токенов: серверные привычки / факты о себе |
| `caps.lore` | `1500` | Лимит токенов: записи лорбука |
| `caps.people` | `9000` | Лимит токенов: другие профили |
| `caps.neighbors` | `3000` | Лимит токенов: соседние каналы |
| `caps.server` | `4000` | Лимит токенов: карта каналов |
| `channelActivity.liveMessagesPerDay` | `20` | Сообщений в день = «активный» канал |
| `channelActivity.deadAfterDays` | `7` | Дней без сообщений = «мёртвый» канал |
| `vision.maxImages` | `4` | Макс. картинок на запрос |
| `vision.tokensPerImage` | `400` | Бюджет токенов на картинку |
| `vision.imageSize` | `512` | Целевой размер после уменьшения (px), через медиа-прокси Discord |
| `vision.recentImages` | `3` | Сколько недавних картинок канала прикладывать |
| `vision.recentImageMinutes` | `30` | Макс. возраст недавних картинок (мин) |
| `vision.maxBytes` | `1500000` | Макс. размер файла картинки (байт); большие пропускаются |
| `vision.fetchTimeoutMs` | `10000` | Таймаут загрузки на картинку (мс) |

## `media`

Настройки модели описаний медиа (`features.mediaDescriptions`).

| Ключ | По умолчанию | Описание |
|---|---|---|
| `model` | `"anthropic/claude-haiku-4.5"` | Модель описаний |
| `maxOutputTokens` | `120` | Макс. токенов на выходе на описание |
| `imageSize` | `512` | Целевой размер после уменьшения (px) |
| `maxPerTurn` | `6` | Макс. описаний за ход |
| `cacheEntries` | `5000` | Размер кэша описаний (ключ: вложение) |
| `filePreviewChars` | `500` | Символов, показываемых из начала текстовых файлов |
| `embedTextChars` | `200` | Символов, показываемых из текста эмбеда ссылки |

## `mention`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `ignoreChance` | `0` | Базовый шанс игнорирования; увеличьте, чтобы персонаж пропускал часть пингов |
| `emptyMentionIgnoreChance` | `0` | Шанс игнорирования пустого @упоминания; увеличьте, чтобы персонаж пропускал часть из них |
| `repeatWindowMinutes` | `10` | Окно отслеживания повторов (мин) |
| `repeatPenalty` | `0` | Добавочный шанс игнорирования за повтор; увеличьте для штрафа за повторы |
| `spamThreshold` | `50` | Вызовов в окне, после которых это спам |
| `spamIgnoreChance` | `0.9` | Шанс игнорирования при спаме |
| `nameTriggerChance` | `1` | Шанс ответа на триггер по имени |
| `neverIgnore` | `[]` | ID пользователей, которые никогда не игнорируются |
| `affinityIgnoreBonus` | `0` | Макс. добавочное игнорирование при отношении -100; увеличьте, чтобы неприятных ему участников игнорировали чаще |
| `affinityLikeBonus` | `0.08` | Макс. снижение игнорирования при отношении +100 |
| `oneAtATime` | `true` | Один ответ за раз на весь сервер |
| `maxPending` | `3` | Сколько каналов могут держать отложенный пинг, пока персонаж занят |
| `pendingMinutes` | `10` | Минут до истечения отложенного пинга |
| `switchDelayMs` | `[2000, 9000]` | Пауза перед ответом в следующем канале (мс) |
| `followUpMinutes` | `2` | Окно продолжения разговора после последнего ответа персонажа (мин) |
| `followUpContext` | `15` | Строк транскрипта, отправляемых классификатору |
| `followUpModel` | `null` | Модель классификатора (`null` = медиа-модель) |
| `followUpMaxOutputTokens` | `8` | Макс. токенов на выходе для классификатора |
| `followUpNoStreak` | `3` | Сколько вердиктов `no` подряд закрывают окно |

## `typing`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `reactionDelayMs` | `[800, 4000]` | Диапазон задержки реакции (мс) |
| `msPerChar` | `[35, 75]` | Скорость набора на символ (мс) |
| `minMs` | `900` | Мин. длительность набора (мс) |
| `maxMs` | `12000` | Макс. длительность набора (мс) |
| `betweenMessagesMs` | `[700, 3500]` | Пауза между сообщениями (мс) |

## `spontaneous`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `channels` | `[]` | Разрешённые каналы |
| `maxChannelSilenceHours` | `72` | Тишина канала, блокирующая спонтанные сообщения (часов); 0 = без лимита |
| `minIntervalMinutes` | `25` | Мин. интервал проверки (мин) |
| `maxIntervalMinutes` | `420` | Макс. интервал проверки (мин) |
| `burstChance` | `0.15` | Шанс серии (ещё одно сообщение вскоре) |
| `burstMinutes` | `[3, 15]` | Задержка внутри серии (мин) |
| `activeHours` | `{ from: 10, to: 3 }` | Активные часы (могут переходить через полночь) |
| `liveWindowMinutes` | `15` | Окно активности (мин) |
| `liveMinMessages` | `4` | Мин. сообщений для «активно» |
| `deadAfterMinutes` | `90` | Тишина до «мертво» (мин) |
| `initiateChance` | `0.35` | Шанс начать тему вместо вклинивания |
| `eavesdropChance` | `0.02` | Шанс вклиниться, на каждое сообщение |
| `eavesdropDelayMs` | `[5000, 40000]` | Диапазон задержки подслушивания (мс) |
| `minGapMinutes` | `12` | Мин. интервал между действиями (мин) |

## `memory`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `model` | `null` | Модель анализатора (`null` = `llm.model`) |
| `mainChannelIds` | `[]` | Каналы, где люди общаются друг с другом; портрет характера и стиля участника строится по ним; пусто означает, что учитываются все каналы |
| `portraitRefreshHours` | `24` | Мин. часов между обновлениями портрета участника |
| `portraitRefreshPerDay` | `20` | Макс. обновлений портрета на сервер в день |
| `batchMessages` | `60` | Идеальный размер батча |
| `minBatchMessages` | `15` | Мин. сообщений перед обновлением |
| `maxBatchAgeMinutes` | `180` | Принудительное обновление по истечении (мин) |
| `maxOutputTokens` | `20000` | Макс. токенов на выходе анализатора |
| `fieldChars` | `1000` | Лимит поля профиля (символов) |
| `clampTolerance` | `1.25` | Текст от анализатора может превышать лимит в это число раз, прежде чем его обрежут; обрез приходится на границу предложения или слова и никогда на ссылку на участника |
| `maxDetails` | `15` | Сколько деталей показывать персонажу и анализатору на профиль |
| `maxDetailsStored` | `40` | Сколько деталей хранить на профиль; показываются лучшие по частоте и новизне |
| `maxInterests` | `12` | Сколько интересов показывать персонажу и анализатору на профиль |
| `maxInterestsStored` | `40` | Сколько интересов хранить на профиль; показываются лучшие по частоте и новизне |
| `interestTopicChars` | `40` | Макс. символов для темы интереса |
| `interestNoteChars` | `120` | Макс. символов для заметки интереса |
| `confirmAfter` | `2` | Наблюдений до подтверждения интереса или детали |
| `confirmGapHours` | `12` | Часов между наблюдениями, чтобы считалось новым случаем |
| `interestStaleDays` | `90` | Дней без наблюдения до пометки интереса как устаревшего |
| `interestHalfLifeDays` | `180` | Период полураспада веса интересов (дней); вес элемента, который не встречался, уменьшается вдвое за каждый период, так что новое увлечение может обогнать старое |
| `detailHalfLifeDays` | `720` | Период полураспада веса деталей (дней) |
| `maxAliases` | `5` | Сколько псевдонимов показывать персонажу и анализатору на профиль |
| `maxAliasesStored` | `15` | Сколько псевдонимов хранить на профиль; показываются лучшие по частоте и новизне |
| `aliasHalfLifeDays` | `365` | Период полураспада веса псевдонимов (дней) |
| `maxInjokes` | `15` | Макс. внутренних шуток сервера |
| `maxSelfFacts` | `20` | Макс. фактов о себе |
| `maxEpisodes` | `20` | Макс. эпизодов на человека |
| `maxNewEpisodes` | `3` | Макс. новых эпизодов на человека за батч |
| `timeoutMs` | `900000` | Таймаут анализатора (мс), отдельный от `llm.timeoutMs` |

Промпт анализатора получает эти лимиты через плейсхолдеры, поэтому увеличение значения вступает в силу со следующего батча. Большие профили дороже по токенам контекста (`context.caps.people`, `context.caps.interlocutor`) и по выводу анализатора (`memory.maxOutputTokens`).

## `relationships`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `damping` | `true` | Демпфировать изменения, уводящие от нуля; изменения к нулю применяются полностью |
| `dampingPower` | `1` | Показатель степени коэффициента демпфирования; чем больше, тем труднее дойти до краёв шкалы |
| `maxDeltaPerUpdate` | `15` | Макс. изменение балла за обновление |
| `historySize` | `10` | Сколько изменений отношения хранить на участника |
| `directTriggerCount` | `6` | Прямых обращений, после которых обновление запускается досрочно |

При включённом `damping` изменение, уводящее балл дальше от нуля, масштабируется как `(1 - |score| / 100) ^ dampingPower`, поэтому до краёв шкалы нужно долго идти; изменение обратно к нулю применяется в полную силу. Балл хранится дробным, а показывается целым; `/nep memory affinity` устанавливает его напрямую без демпфирования.

## `lore`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `maxEntries` | `500` | Макс. записей лорбука на сервер |
| `scanMessages` | `30` | Сообщений, сканируемых на совпадение ключей |
| `maxMatches` | `8` | Макс. записей, показываемых за запрос |
| `textChars` | `600` | Лимит текста записи лорбука (символов) |

## `warmup`

| Ключ | По умолчанию | Описание |
|---|---|---|
| `enabled` | `true` | Запускать прогрев автоматически при первом старте |
| `lookbackDays` | `60` | Глубина выборки (дней) |
| `minMessages` | `30` | Мин. собственных сообщений, чтобы участник попал в прогрев |
| `maxPeople` | `40` | Обрабатываемых участников, самые активные первыми |
| `messagesPerPerson` | `2000` | Собственных сообщений, выбираемых на участника |
| `contextBefore` | `1` | Строк контекста перед каждым выбранным сообщением |
| `maxChannelShare` | `0.5` | Макс. доля выборки из одного канала |
| `messagesPerChannel` | `200` | Последних сообщений, по которым описывается канал |
| `serverSampleMessages` | `600` | Недавних сообщений из основных каналов для серверного запроса |
| `refreshMessages` | `400` | Сообщений, выбираемых для обновления портрета |
| `fetchLimitPerChannel` | `15000` | Сообщений, загружаемых на канал для пула выборки |
| `maxOutputTokens` | `6000` | Макс. токенов на выходе за запрос прогрева |
| `maxRequestTokens` | `120000` | Макс. токенов на запрос прогрева (вход + выход) |
| `maxTokens` | `6000000` | Общий бюджет токенов на прогон |
| `rateLimitWaitMinutes` | `10` | Минут ожидания при ограничении частоты запросов |
| `rateLimitMaxWaits` | `36` | Последовательных ожиданий до прерывания прогона |
